part of '../{{name.snakeCase()}}_page.dart';

class _{{name.pascalCase()}}BodySection extends StatelessWidget {
  const _{{name.pascalCase()}}BodySection();

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('{{name.pascalCase()}}'),
    );
  }
}
